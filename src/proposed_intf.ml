(** Proposed interface *)

(** *)

(* Let's say that we have a default interface that uses mtime, mtime.clock and
   mtime.clock.os (ignoring jsoo for now). Then we also have an interface that uses JS
   TSC as the timer. *)
