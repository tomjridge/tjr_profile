(library
  (name tjr_profile)
  (public_name tjr_profile)
  (preprocessor_deps (file "config.ml"))
  (preprocess (pps ppx_optcomp))
  (libraries ppx_optcomp core core.time_stamp_counter)  ;; core for TSC
)
