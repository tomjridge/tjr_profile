(library
  (name tjr_profile)
  (public_name tjr_profile)
  (flags (:standard -open Tjr_lib_core))
  (preprocessor_deps (file "profiling_optcomp_config.ml"))
  (preprocess (pps ppx_optcomp))
  (libraries tjr_lib_core ppx_optcomp core)  ;; core for TSC
)
